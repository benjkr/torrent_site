# AGENTS.md

This project uses **bun** exclusively as its JavaScript runtime and package manager.

- Use `bun run <script>` for all package.json scripts
- Use `bun install` to install dependencies
- Use `bun <command>` for running TypeScript files directly
- Do not use npm, yarn, or pnpm
