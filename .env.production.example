# Copy to .env.production and fill in real values. Never commit .env.production.
NODE_ENV=production
QB_BASE_URL=http://your-qbittorrent-host:8080/
QB_USERNAME=
QB_PASSWORD=
# GitHub PAT (or token) with packages:read/write for ghcr.io — used by publish.sh
GH_TOKEN=
